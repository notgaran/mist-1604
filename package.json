{
  "name": "my-vue-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^4.4.0"
  },
  "dependencies": {
    "firebase": "^9.23.0"
  }
}
